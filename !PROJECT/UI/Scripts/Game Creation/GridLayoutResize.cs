using UnityEngine;
using UnityEngine.UI;

public class GridLayoutResize : MonoBehaviour {
    //public int nrButoanePeLinie = 3;
	
    //public void Resize(int nrOfButtonsSpawned) {
    //    RectTransform rectTransform = GetComponent<RectTransform>();
    //    GridLayoutGroup gridLayout = GetComponent<GridLayoutGroup>();

    //    float neededLength = nrOfButtonsSpawned / nrButoanePeLinie * (gridLayout.cellSize.y + gridLayout.spacing.y) - gridLayout.spacing.y;
    //    if (neededLength < 0)
    //        neededLength = gridLayout.cellSize.y;
    //    rectTransform.sizeDelta = new Vector2(rectTransform.sizeDelta.x, neededLength);
    //}
}
